package com.quzzar.server.worldutils;


public class PhysicsBlocks {

	/*
  public static List<Material> physicsMaterials = Arrays.asList(new Material[] { 
		  Material.GRASS_BLOCK, Material.DIRT, Material.PODZOL, Material.MYCELIUM, Material.COARSE_DIRT, Material.FARMLAND });

  
  public static void update(Block b) {
    if (physicsMaterials.contains(b.getType())){
      if (!b.getRelative(BlockFace.DOWN).getType().isSolid()) {
        
        Location centerBlockLoc = b.getLocation().clone();
        centerBlockLoc.setX(centerBlockLoc.getX() + 0.5D);
        centerBlockLoc.setZ(centerBlockLoc.getZ() + 0.5D);
        
        FallingBlock fallingblock = b.getWorld().spawnFallingBlock(centerBlockLoc, b.getBlockData());
        fallingblock.setDropItem(false);
        
        b.setType(Material.AIR);
      } 
    }
  }
  */
  
}